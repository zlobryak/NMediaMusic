<?xml version="1.0" encoding="utf-8"?>
<androidx.constraintlayout.widget.ConstraintLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:background="?attr/selectableItemBackground"
    android:paddingHorizontal="16dp"
    android:paddingVertical="12dp">

    <!-- Номер трека (показывается для неактивных треков) -->
    <TextView
        android:id="@+id/trackNumber"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:gravity="center"
        android:textSize="16sp"
        android:textColor="@color/text_secondary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toStartOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="1" />

    <!-- Иконка Play/Pause (показывается для активного трека) -->
    <ImageView
        android:id="@+id/trackPlayIcon"
        android:layout_width="24dp"
        android:layout_height="24dp"
        android:visibility="gone"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintStart_toEndOf="@id/trackNumber"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/icon_color"
        tools:src="@drawable/ic_play"
        tools:visibility="visible" />

    <!-- Название трека -->
    <TextView
        android:id="@+id/trackTitle"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="16sp"
        android:textColor="@color/text_primary"
        app:layout_constraintBottom_toTopOf="@id/trackArtist"
        app:layout_constraintEnd_toStartOf="@id/trackPlayingIndicator"
        app:layout_constraintStart_toEndOf="@id/trackPlayIcon"
        app:layout_constraintTop_toTopOf="parent"
        app:layout_constraintVertical_chainStyle="packed"
        tools:text="Track Title" />

    <!-- Исполнитель трека -->
    <TextView
        android:id="@+id/trackArtist"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_marginStart="16dp"
        android:layout_marginTop="4dp"
        android:layout_marginEnd="8dp"
        android:ellipsize="end"
        android:maxLines="1"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/trackPlayingIndicator"
        app:layout_constraintStart_toEndOf="@id/trackPlayIcon"
        app:layout_constraintTop_toBottomOf="@id/trackTitle"
        tools:text="Artist" />

    <!-- Индикатор воспроизведения (эквалайзер) -->
    <ImageView
        android:id="@+id/trackPlayingIndicator"
        android:layout_width="20dp"
        android:layout_height="20dp"
        android:layout_marginEnd="8dp"
        android:visibility="gone"
        android:contentDescription="Now playing"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toStartOf="@id/trackDuration"
        app:layout_constraintTop_toTopOf="parent"
        app:tint="@color/accent"
        tools:src="@drawable/ic_equalizer"
        tools:visibility="visible" />

    <!-- Длительность трека -->
    <TextView
        android:id="@+id/trackDuration"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:textSize="14sp"
        android:textColor="@color/text_secondary"
        app:layout_constraintBottom_toBottomOf="parent"
        app:layout_constraintEnd_toEndOf="parent"
        app:layout_constraintTop_toTopOf="parent"
        tools:text="4:13" />

</androidx.constraintlayout.widget.ConstraintLayout>